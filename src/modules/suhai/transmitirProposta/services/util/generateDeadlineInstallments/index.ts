import { container } from "@modules/suhai/transmitirProposta/services/util/generateDeadlineInstallments/container";

export const generateDeadlineInstallments = {
    generate: () => container().generate()
}